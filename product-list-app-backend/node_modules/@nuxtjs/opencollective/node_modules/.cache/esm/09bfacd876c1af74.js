let reportAndThrowError,stripLeadingSlash,stripTrailingSlash,fetchLogo,fetchPkg,fetchStats;_76c‍.x([["collectiveSlugFromUrl",()=>collectiveSlugFromUrl],["collectiveUrl",()=>collectiveUrl],["collectiveLogoUrl",()=>collectiveLogoUrl],["collectiveDonationText",()=>collectiveDonationText],["getCollective",()=>getCollective],["collectiveDonationUrl",()=>collectiveDonationUrl],["retrieveDonationSlug",()=>retrieveDonationSlug]]);_76c‍.w("./misc",[["reportAndThrowError",["reportAndThrowError"],function(v){reportAndThrowError=v}],["stripLeadingSlash",["stripLeadingSlash"],function(v){stripLeadingSlash=v}],["stripTrailingSlash",["stripTrailingSlash"],function(v){stripTrailingSlash=v}]]);_76c‍.w("./fetch",[["fetchLogo",["fetchLogo"],function(v){fetchLogo=v}],["fetchPkg",["fetchPkg"],function(v){fetchPkg=v}],["fetchStats",["fetchStats"],function(v){fetchStats=v}]]);


       const collectiveSlugFromUrl = url => url.substr(url.lastIndexOf('/') + 1).toLowerCase().replace(/\.json/g, '');_76c‍.j(["collectiveSlugFromUrl"]);

       const collectiveUrl = pkg => {
  const url = pkg.collective && pkg.collective.url

  if (!url) {
    _76c‍.a("reportAndThrowError",reportAndThrowError)('No collective URL set!')
  }

  return _76c‍.a("stripTrailingSlash",stripTrailingSlash)(url)
};_76c‍.j(["collectiveUrl"]);

// use pkg.collective.logo for "legacy"/compatibility reasons
       const collectiveLogoUrl = pkg => pkg.collective.logo || pkg.collective.logoUrl || false;_76c‍.j(["collectiveLogoUrl"]);

       const collectiveDonationText = pkg => (pkg.collective.donation && pkg.collective.donation.text) || 'Donate:';_76c‍.j(["collectiveDonationText"]);

       const getCollective = async pkgPath => {
  const pkg = _76c‍.a("fetchPkg",fetchPkg)(pkgPath)
  const url = collectiveUrl(pkg)
  const baseCollective = {
    url,
    slug: collectiveSlugFromUrl(url),
    logoUrl: collectiveLogoUrl(pkg),
    donationUrl: collectiveDonationUrl(pkg),
    donationText: collectiveDonationText(pkg)
  }
  const logoUrl = baseCollective.logoUrl
  const promises = [_76c‍.a("fetchStats",fetchStats)(url)].concat(logoUrl ? _76c‍.a("fetchLogo",fetchLogo)(logoUrl) : [])

  const [stats, logo] = await Promise.all(promises)

  return Object.assign(baseCollective, { stats, logo })
};_76c‍.j(["getCollective"]);

       const collectiveDonationUrl = pkg => {
  const defaultDonationAmount = pkg.collective.donation && pkg.collective.donation.amount

  let donateUrl = `${collectiveUrl(pkg)}/${retrieveDonationSlug(pkg)}`

  if (defaultDonationAmount) {
    return `${donateUrl}/${defaultDonationAmount}`
  }

  return donateUrl
};_76c‍.j(["collectiveDonationUrl"]);

       const retrieveDonationSlug = pkg => {
  const rawDonationSlug = (pkg.collective.donation && pkg.collective.donation.slug)

  if (!rawDonationSlug) {
    return 'donate'
  }

  return _76c‍.a("stripLeadingSlash",stripLeadingSlash)(rawDonationSlug)
};_76c‍.j(["retrieveDonationSlug"]);
